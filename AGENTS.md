# AGENTS.md

## SamRoadChange

SamRoadChange 是一个面向多区域、多时相遥感影像的道路提取、道路面提取、道路测宽、道路变化检测、人工编辑和长时序分析桌面软件。

项目主要使用 Python，GUI 当前基于 Tkinter。

---

## 1. 代码架构

项目当前已有明确分层，应将其作为定位代码和维护职责边界的基础。目录与内部实现不是不可变约束；任务确有需要时，可以在保持职责方向的前提下拆分、合并、重写或重新组织相关模块。

### GUI 层

`code/gui/`

负责：

- Tkinter 界面布局
- 用户交互
- 页面状态显示
- 进度、日志和结果展示

主要文件：

- `main_window.py`：主窗口、全局 UI 状态、页面组合和应用级 GUI 协调
- `data_page.py`：数据准备页面
- `run_page.py`：运行任务和局部重跑页面
- `edit_page.py`：人工编辑页面
- `result_page.py`：成果与精度评价页面
- `common_widgets.py`：通用 UI 常量、尺寸、控件辅助函数

`UserApp` 由多个页面类组合而成，因此页面文件中的 callback 可能实际定义在 `main_window.py`。

GUI 页面不要直接实现道路提取、测宽或变化检测算法。

---

### 应用层

`code/app/`

负责 GUI 与后端之间的业务协调。

主要文件：

- `backend_client.py`
  - GUI 与 `user_pipeline.py` 的 subprocess 边界
  - 后端启动、取消、stdout 日志和结构化事件
  - 不包含 Tkinter 界面逻辑

- `task_manager.py`
  - 将“完整运行、期次重跑、变化对重跑、编辑后更新、评价”等用户意图转换为后端命令
  - 处理续跑状态和上下游依赖关系

- `project_manager.py`
  - 项目配置
  - 外部数据扫描
  - 数据源管理
  - 项目结果索引

- `editor_manager.py`
  - 人工编辑器启动与管理

- `result_publisher.py`
  - 项目成果目录和结果索引/发布

修改 GUI 功能时，应优先通过这些应用层接口完成，不要绕过它们直接调用算法。

---

### 后端流水线

`code/user_pipeline.py`

这是主要批处理后端和流水线调度入口。

负责串联：

道路中心线提取
→ 道路面提取
→ 道路宽度计算
→ 结果固化
→ 产品导出
→ 相邻期变化检测
→ 长时序更新
→ 人工编辑后重建

这是大型核心文件。

**GUI 布局、控件文字、页面组织等修改默认不要读取或修改本文件。**

只有任务涉及以下内容时才读取相关部分：

- 后端 CLI 命令
- pipeline 阶段行为
- 成果生成逻辑
- 变化检测调用
- 人工编辑后重建流程
- 后端结构化事件协议

不要为了“理解项目”完整阅读 `user_pipeline.py`。

---

### 算法层

`code/engine/`

主要包括：

- `engine/samroad/`：道路中心线提取
- `engine/sam_molra/`：道路面相关算法
- `engine/width/`：道路宽度计算

除非任务明确要求修改算法，否则不要读取这些目录。

尤其是纯 GUI、项目管理或任务调度修改时，禁止为了建立上下文递归扫描 `engine/`。

---

### 其他重要文件

- `code/input_catalog.py`
  - 输入数据、期次排序和路径列表相关辅助功能

- `code/temporal_road_analysis.py`
  - 长时序道路分析

- `code/user_workflow_gui.py`
  - 旧入口兼容层
  - 正常情况下不要在此实现新 GUI 功能

- `code/tests/`
  - 测试
  - 优先运行与修改模块相关的测试，不默认运行全部测试

- `runtime/`
  - 环境、模型、配置等运行资源
  - 普通代码修改不要扫描

- `project/`
  - 本地项目输入与运行成果
  - 不属于源代码
  - 普通开发任务不要扫描

---

## 2. Codex 默认工作方式

本仓库默认采用 **最小必要上下文 + 按问题选择修改策略** 的开发模式。

开始任何修改前：

1. 读取本文件 `AGENTS.md`
2. 查看 `git status`
3. 查看 `git diff --stat`
4. 查看本次已有 diff
5. 阅读 `docs/CODEMAP.md`
6. 根据任务定位最小相关文件集合
7. 只读取相关文件及其直接调用方/直接依赖
8. 信息不足时再逐步扩大范围

任务开始前已经存在的已跟踪和未跟踪修改都视为用户工作。除非需求明确涉及，不要覆盖、回退、重排格式或顺带合入这些修改。

**不要默认递归扫描整个仓库。**

不要因为“先理解一下项目”而遍历所有 Python 文件。

这里最小化的是无关代码阅读范围，不是必要的修改范围，也不是实现质量。现有代码是理解问题的上下文，不是必须保留的设计约束。

定位相关实现和必要调用链后，根据实际情况选择：

- **小范围修复**：当前设计合理，问题确实局部，且不会增加额外复杂度；
- **局部重构**：存在明显重复、条件分支过多、职责放错层或内部接口阻碍新功能；
- **模块重写**：模块核心思路已不适用，旧实现充斥 fallback、heuristic 或兼容逻辑，继续修改比替换更复杂；
- **架构调整**：当前层级或数据流实质性阻碍需求，保留它需要大量 adapter、bridge、旁路或重复流程。

允许删除废弃实现、同步调整直接调用方和测试。不要把“小范围修改”当作高于代码清晰度和需求正确性的默认目标。

---

## 3. 按任务限制读取范围

### 如果只是修改 GUI 布局或文字

默认只读取：

- 对应的 `code/gui/*_page.py`
- 必要时 `code/gui/common_widgets.py`
- 必要时 `code/gui/main_window.py` 中相关方法

默认不要读取：

- `code/user_pipeline.py`
- `code/engine/`
- `runtime/`
- 项目运行成果
- 算法测试

---

### 如果修改 GUI 按钮行为

先读取：

- 对应 GUI 页面
- `main_window.py` 中对应 callback
- 对应的 `code/app/*_manager.py`

只有应用层接口不足时，才继续读取 `user_pipeline.py` 对应命令实现。

---

### 如果修改项目扫描或数据组织

优先读取：

- `code/app/project_manager.py`
- `code/input_catalog.py`
- 对应 GUI 数据页面

不要读取算法层。

---

### 如果修改完整运行、续跑或局部重跑

优先读取：

- `code/app/task_manager.py`
- `code/app/backend_client.py`
- 对应 GUI 页面和 callback

只有需要改变后端实际执行行为时，才读取 `user_pipeline.py` 对应命令。

---

### 如果修改道路提取、道路面或测宽算法

再进入：

- `code/user_pipeline.py` 对应调用位置
- 对应 `code/engine/` 子模块
- 对应测试

不要顺便修改 GUI，除非接口变化确实需要。

---

## 4. 架构约束

保持以下调用方向：

```text
GUI
↓
app manager
↓
BackendClient / backend command
↓
user_pipeline.py
↓
engine algorithms
```

不要新增：

```text
GUI → engine
```

直接调用。

GUI 不应知道 SAMRoad、SAM-MLoRA 或 width 内部实现细节。

算法层也不应依赖 Tkinter。

---

## 5. 修改原则

优先：

- 保持实现清晰和职责边界明确
- 减少重复逻辑与不必要的条件分支
- 删除、合并或替换废弃实现
- 避免兼容层、fallback 和 heuristic 持续堆叠
- 保持用户明确要求的稳定外部接口
- 保持断点续跑能力
- 保持多区域、多时相能力

**历史实现不是需求。** 除非用户明确要求向后兼容，或相关行为属于稳定外部接口，否则不要仅为了保留旧实现而新增：

- compatibility branch
- legacy fallback
- wrapper、adapter 或 bridge
- parallel execution path
- duplicated processing pipeline
- 特殊模式专用补丁
- 多层 heuristic

如果旧逻辑已经与当前目标冲突，优先考虑：

```text
删除 → 简化 → 合并 → 重构 → 替换
```

不要继续叠加一层新逻辑。如果现有内部接口或实现本身是复杂度来源，应重构它并同步修改直接调用方，而不是再包一层抽象。

对稳定外部接口优先保持兼容；内部接口如果已经妨碍实现，可以同步重构其调用方和测试。已有 manager 职责合理时复用；如果 manager 承担了错误职责或导致重复调用链，应直接重构，而不是增加旁路接口。

不要因为局部需求进行**无关的**大规模重构，但也不要以控制任务范围为由回避完成目标所必需的局部重构、模块重写或架构调整。

当用户明确使用“重新设计”“重新实现”“重构”“推翻当前实现”“不要兼容旧逻辑”“重新考虑这个模块”“现在的方案不合理”“不要继续打补丁”等表达时，视为允许删除或替换相关内部实现。此时不要默认采用增量兼容策略。

除非任务明确要求，否则：

- 不移动大量文件
- 不更改成果目录格式
- 不改变已有 CLI 参数
- 不改变结果 manifest 格式
- 不修改模型算法
- 不无关地改变已经验证工作的 pipeline 行为

这些约束用于控制任务范围和保护稳定外部契约，不代表必须保留造成复杂度的内部实现。

---

## 6. 测试原则

修改完成后：

1. 先检查语法和导入
2. 运行与修改模块直接相关的测试
3. 根据实际影响范围扩大测试

不要默认执行所有耗时算法、模型推理或完整遥感流水线。

只有任务明确要求端到端验证时才运行完整流程。

---

## 7. 大目录与高成本文件

没有明确理由时不要扫描：

- `.git/`
- `runtime/`
- `project/`
- `models/`
- 环境目录
- 缓存目录
- 输出成果目录
- 日志目录

`code/user_pipeline.py` 也属于高成本文件，应按需要局部读取，不要默认全文读取。

---

## 8. 修改完成后的回答

完成任务后简要说明：

- 修改了哪些文件
- 修改目的
- 是否改变接口
- 运行了哪些测试
- 是否存在未验证部分

不要重新复述整个项目架构。
