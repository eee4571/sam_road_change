# 实际任务变化检测问题与代码优化

## 1. 目标

本轮优化面向多期道路提取后的实际生产任务。输出仍全部采用 SHP；“没有变化”“需要复核”或“输入损坏”都必须有明确、可追溯的表达，不能依赖文件是否存在来猜测。

## 2. 主要风险与处理

| 问题 | 可能结果 | 当前处理 |
|---|---|---|
| 影像配准偏移、提取线漂移 | 同一路段误判为新增或消失 | 使用米制容差、双向覆盖率、距离和方向联合匹配 |
| 路口有垂直或斜交道路 | 跨期道路串号 | 增加道路方向相似度，垂直道路不再仅凭距离匹配 |
| 平行道路、双幅路、重复中心线 | 一个源对象被多个道路 ID 占用 | 对强重叠引用检查重复归属，较弱对象进入复核层 |
| 云、阴影或模型单期漏检 | 产生“消失后又出现”的伪事件 | 单期 `存在—缺失—存在` 自动降级为 `uncertain` |
| 只凭状态转移判定事件 | 新增/消失可能缺少两期变化面支持 | 记录证据状态；没有对应变化面的事件进入人工复核 |
| 无变化时不生成 SHP | 下游无法区分无变化和运行失败 | 固定生成 6 个两期变化 SHP，允许为空但可读取 |
| 同一目录重跑 | 上一次的变化文件残留 | 写入前清理固定文件名的 SHP 侧车文件和旧 GPKG |
| SHP 缺 DBF/SHX 或 CRS | 静默跳过，表面成功但成果损坏 | 对真实成果明确报错并列出缺失文件 |
| SHP 字段名最多 10 字符 | 多期字段截断或重名 | 使用唯一短字段名，如 `S2021`、`W2021`、`C2021` |
| 道路拆分、合并、改线 | 单一稳定 ID 无法表达完整血缘 | 已保留 `road_lineage.shp`；复杂血缘列为下一阶段 |

## 3. 已实施优化

### 3.1 两期变化 SHP 输出契约

每个两期变化目录固定输出：

- `added_roads.shp`
- `removed_roads.shp`
- `widened_road_parts.shp`
- `narrowed_road_parts.shp`
- `road_changes.shp`
- `unchanged_road_surfaces.shp`

没有相应对象时生成可读取的空 SHP。重跑前只删除上述同名 SHP 的已知侧车文件，不触碰目录内其他文件。

### 3.2 长时序匹配质量控制

匹配分数由双向覆盖率、方向相似度和距离接近度共同组成。方向采用沿线采样点的主方向计算，线坐标正反顺序不影响结果。

`road_obs.shp` 新增：

- `dir_sim`：方向相似度，范围 0～1；
- `qa_reason`：复核原因，例如单期漏检或重复源归属。

`road_event.shp` 的 `evidence` 字段区分：

- `temporal_state_and_pair_change`：状态转移和两期变化面一致；
- `state_only_no_pair_part`：有两期成果，但没有对应变化面；
- `state_only_no_pair_input`：未提供两期变化成果，仅依据长时序状态。

后两类事件设置 `qa_state=review`，并同步写入 `road_review.shp`。

### 3.3 错误显式化

单期中心线或两期变化 SHP 缺少 `.shp/.shx/.dbf`，或变化 SHP 缺少 CRS 时，流程不再静默跳过，而是明确报错。零字节占位文件仅为现有模拟测试保留兼容处理，不属于正式成果。

## 4. 生产质检建议

1. 所有期次先统一正射校正、投影和裁剪范围；容差按分辨率与实测配准误差设置，不宜单纯调大。
2. 首批样区覆盖城区密路网、乡村稀疏路、山地弯曲路、双幅路和施工区。
3. 优先复核 `road_review.shp`，重点检查 `duplicate_source_ownership`、`single_period_dropout` 和 `state_only_no_pair_part`。
4. 按新增、消失、宽度变化分别统计对象级精确率和召回率，避免只看总面积。
5. 正式任务目录按任务或版本隔离，不要混放人工编辑文件和自动中间成果。

## 5. 后续优先级

### P1：拆分、合并和改线血缘

实现一对多、多对一匹配，将 `split`、`merge`、`rerouted` 写入 `road_lineage.shp`，避免将复杂变化简化为多条新增/消失。

### P1：影像有效覆盖掩膜

为每期引入有效观测范围。云、NoData 和遮挡区域应记为 `no_data`，不能记为 `absent`。

### P2：全局匹配与大数据分块

超密路网采用分块二分图或最小费用匹配，联合约束距离、方向、拓扑邻接和长度覆盖。大型验证区按格网分块并在边界去重，输出仍使用 SHP。

## 6. 验收条件

- 两期完全无变化时，6 个 SHP 均存在且变化层要素数为 0；
- 同目录先运行有变化、再运行无变化，不得读到旧变化对象；
- 垂直交叉但不连续的道路不得共享稳定道路 ID；
- 单期漏检进入 `road_review.shp`，不得生成确定的消失和新增事件；
- 正式 SHP 缺少 DBF、SHX 或 CRS 时必须明确失败；
- 全部既有单元测试和新增边界测试通过。
